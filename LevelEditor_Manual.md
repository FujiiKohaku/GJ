# レベルエディタ（EditorScene）操作マニュアル

このドキュメントは、チームメンバー向けに `EditorScene` および外部のパレットツール（`editor_tool.py`）を用いたステージの作成・編集方法をまとめたものです。

## 1. エディタの起動と基本設定
1. ゲーム内で「エディタモード」に移行（ArchiveSceneからの遷移等）すると `EditorScene` が開始されます。
2. 同時にバックグラウンドで Python 製の外部ツール (`editor_tool.py`) が起動し、専用のウィンドウ（Map Editor）が表示されます。
3. エディタ画面と外部ツールは UDP 通信で連携しており、ツールのボタン操作でロード、セーブ、および配置するブロック（パレット）の切り替えが行われます。

## 2. カメラ操作（視点の移動）
エディタ画面では以下の操作でカメラを動かすことができます。
- **平行移動（パン）**：`Shift` + 左ドラッグ、または `W`, `A`, `S`, `D` キー（および矢印キー）
- **ズーム（拡大・縮小）**：マウスホイールのスクロール

## 3. 基本操作（ブロック・ギミックの配置と削除）
エディタ画面上のグリッドに合わせて、クリック操作でマップチップやギミックを配置できます。

- **配置（ペイント）**：`左クリック`
  - 左クリックしたままドラッグすることで、連続して配置することが可能です。
- **削除（消しゴム）**：`右クリック`
  - 配置済みのブロックやギミックを削除し、空白（Air）にします。
- **オブジェクトの移動**：`左クリック + ドラッグ`
  - 自機（Player Spawn）やゴール（Goal）など、一部のギミックは左クリックしたままドラッグすることで位置を移動させることができます。
- **プロパティの選択**：`Ctrl` + `左クリック`
  - ギミックを選択します。選択されたギミックのパラメータが画面右側の ImGui パネルに表示され、編集可能になります。
- **Undo（元に戻す）**：`Ctrl` + `Z`
- **Redo（やり直す）**：`Ctrl` + `Shift` + `Z` または `Ctrl` + `Y`

## 4. ファイル操作（外部ツール）
外部ツールの「File Operations」パネルから以下の操作を行います。
- **Load JSON**：既存のステージデータ（例: `stage1.json`）を読み込みます。
- **Save JSON**：現在の編集内容をJSONファイルとして保存します（保存時に自動で余白がトリミングされます）。

## 5. 配置可能なパレット一覧
外部ツールの「Palette (Block Type)」から配置したい要素を選びます。

| ID | パレット名 | 概要 |
|:---|:---|:---|
| 0 | **Air (Delete)** | 空白（実質的な削除ツール） |
| 1 | **Floor (Block)** | 通常の床・壁ブロック |
| 2 | **Moving Block (Gimmick)** | 動く床 |
| 3 | **Spike** | 即死トゲ |
| 4 | **Goal** | ゴール地点 |
| 5 | **Switch (Pressure Plate)** | 踏むとイベントを発火するスイッチ |
| 6 | **Gas Emitter** | 毒ガスを噴射するトラップ |
| 7 | **Switch (Bonfire)** | 攻撃を当てると発火する篝火スイッチ |
| 8 | **Destructible Wall** | 破壊可能な壁 |
| 9 | **Laser Emitter** | レーザー発射機 |
| 10 | **Swinging Bridge** | 揺れる橋 |
| 11 | **Wall** | 背景用の壁など |
| 12 | **Door** | イベントで開閉するドア |
| 13 | **土台 (Foundation)** | 床の装飾用 |
| 14 | **Gear (Obstacle)** | 回転する歯車の障害物 |
| 15 | **Checkpoint (Respawn)** | 通過すると、次の命のリレー時の復帰地点をここへ更新 |
| 99 | **Player Spawn** | プレイヤーの初期スポーン地点 |

## 6. ギミックの詳細パラメータ
`Ctrl` + `左クリック` でギミックを選択すると、画面右側に「Gimmick Properties」が表示されます。ここでギミックごとの固有の動きや連携イベントを設定できます。

> [!TIP]
> **連携イベント（Event）について**
> `Switch` が発火する **Fire Event Name** と、`Door` や `Gas Emitter` が受け取る **Listen Event Name** に同じ文字列を設定することで、「スイッチを踏んだらドアが開く」といった連動ギミックを作ることができます。連携が設定されると、エディタ上では両者を結ぶオレンジ色の線で可視化されます。

### 各ギミックのパラメータ詳細

#### 共通ギミック
- **Moving Block (動く床)**
  - `Speed`: 移動するスピード。
  - `Move Range (Blocks)`: 何ブロック分移動するか。
  - `Move Direction`: 移動する方向。
- **Swinging Bridge (揺れる橋)**
  - `Rope Length`: 紐の長さ。
  - `Swing Range (Blocks)`: 左右に揺れる振り幅。
  - `Swing Speed`: 揺れる速度。
  - `Initial Phase`: 初期位相（0.0〜1.0で開始時の揺れの位置をずらす）。

#### トラップ・障害物
- **Gear (歯車)**
  - `Rotation Speed (deg/s)`: 回転速度（度/秒）。マイナス値で逆回転。
  - `Scale`: 歯車の描画および当たり判定の拡大率。
  - `Collision Radius`: 当たり判定（円）の基本半径。
- **Laser Emitter (レーザー)**
  - `Direction`: レーザーを発射する方向（Up, Down, Left, Right）。
  - `Max Distance`: レーザーが届く最大距離（ブロック数）。
- **Gas Emitter (毒ガス)**
  - `Listen Event Name`: 紐づけるスイッチのイベント名。
  - `Gas Range (Blocks)`: 上下左右それぞれに何ブロック分ガスを噴射するか（Left, Right, Up, Down）。
- **Spike (トゲ)**
  - *※全トゲ共通のグローバル設定として `AABB Size` や `AABB Offset` の微調整が可能です。*

#### 相互作用（イベント系）
- **Switch (スイッチ全般)**
  - `Fire Event Name`: このスイッチが押された時に発火するイベント名。
  - `Switch Type`: 種類（0: Pressure Plate=感圧板, 1: Bonfire=篝火）。
  - *(Pressure Plate時)* `Required Weight`: スイッチを起動するのに必要な重さ（スライムの死体数など）。
  - *(Bonfire時)* `[Global Settings (Bonfire)]`: 炎エフェクトの表示オフセットやスケール（共通設定）。
- **Door (ドア)**
  - `Listen Event Name`: 紐づけるスイッチのイベント名。
  - `Height (Blocks)`: ドアの高さ（何ブロック分ふさぐか）。

---
> [!NOTE]
> このドキュメントは随時更新されます。新しいギミックを追加した場合は、このマニュアルにもパラメータの使い方を追記してください。
